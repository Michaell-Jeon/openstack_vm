sudo apt-get install node -y
sudo apt-get install npm -y
sudo npm install -g locally
locally

sudo passwd root

adduser stack
passwd stack

apt-get install -y || yum install -y sudo
echo "stack ALL=(ALL) NOPASSWD:ALL" >> /etc/sudoers
su - stack
cd

apt-get install git -y || yum install -y git

git clone -b stable/juno git://github.com/openstack-dev/devstack.git

git clone git://github.com/Michaell-Jeon/openstack_vm.git

cp localrc.neutron ./devstack/localrc
cd ./devstack
./stack.sh


